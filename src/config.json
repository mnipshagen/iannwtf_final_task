{
    "storage" : {
        "folder"    : "storage/",
        "id_file"   : "recipes.csv",
        "json_file" : "recipes.json",
        "puretext"  : "all_words.txt",
        "database"  : "iannwtf",
        "collection": "recipes"
    },
    "network": {
        "hyperparameters": {
            "embedding_size": 64,
            "dropout_rate": 0.85,
            "z_size": 50,
            "max_ingredients": 10
        },
        "directories": {
            "embedding_weights": "embedding_weights/",
            "weight_dir": "weights/",
            "summary_dir": "summary/"
        }
    }
}