{
    "storage" : {
        "folder"    : "database/",
        "id_file"   : "recipes.csv",
        "json_file" : "recipes.json",
        "puretext"  : "all_words.txt",
        "database"  : "iannwtf",
        "collection": "recipes"
    },
    "network": {
        "hyperparameters": {
            "embedding_size": 64,
            "dropout_rate": 0.85,
            "z_size": 50,
            "max_ingredients": 10
        },
        "directories": {
            "embedding_weights": "train_data/embedding_weights/",
            "weight_dir": "train_data/weights/",
            "summary_dir": "train_data/summary/"
        }
    }
}